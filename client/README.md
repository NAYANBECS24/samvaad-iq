# SAMVAAD-IQ Client

React + Vite frontend for the SAMVAAD-IQ prototype.

```powershell
npm install
npm run dev
npm run lint
npm run build
```

The client currently uses `src/data/demoSeed.json` as a reliable local fallback for the hackathon demo flow.
