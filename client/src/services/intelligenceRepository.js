// Canonical browser repository facade. The implementation remains in the
// compatibility module while existing integrations migrate without breaking
// deep links or offline behavior. Both paths use the shared KAVACH domain core.
export * from './prototypeEngine.js'
