{
  "name": "samvaad-iq-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "SAMVAAD-IQ Zoho Catalyst-ready prototype workspace",
  "scripts": {
    "start": "npm run client:dev",
    "dev": "npm run client:dev",
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "client:lint": "npm --prefix client run lint",
    "api:dev": "node functions/api/index.js",
    "api:smoke": "node functions/api/scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
